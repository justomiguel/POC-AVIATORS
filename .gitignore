# OS
.DS_Store
Thumbs.db

# Secrets & local env (never commit)
.env
.env.*
*.pem
client_secret*.json
.clasprc.json

# Logs & deps (if added later)
*.log
node_modules/

# Optional: deployment metadata (uncomment if you do not want deployment IDs in git)
# gas/deploy.json
